<%= form_for(@nonuser) do |f| %>
  <% if @nonuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nonuser.errors.count, "error") %> prohibited this nonuser from being saved:</h2>

      <ul>
      <% @nonuser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
