<%= form_for(@contender) do |f| %>
  <% if @contender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contender.errors.count, "error") %> prohibited this contender from being saved:</h2>

      <ul>
      <% @contender.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contendent %><br />
    <%= f.text_field :contendent %>
  </div>
  <div class="field">
    <%= f.label :contendent_type %><br />
    <%= f.text_field :contendent_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
